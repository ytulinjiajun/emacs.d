

			



;;禁止自动匹配单引号
(sp-local-pair 'emacs-lisp-mode "'" nil :actions nil)

;;该配置使得每个分屏都有唯一的一个编号
(require 'powerline)
(powerline-default-theme)

;;该配置使得可以使用alt+数字来切换到不同的分屏
(window-numbering-mode 1)

(provide 'config-base-plugin)
