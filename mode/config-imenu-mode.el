;;------------------------------------------------------------------
;; 按下M-s i时，会在小缓冲区中列举出当前buffer下面的所有函数，可以跳转到这些函数
;;------------------------------------------------------------------
(global-set-key (kbd "M-s i") 'counsel-imenu)

(provide 'config-imenu-mode)
